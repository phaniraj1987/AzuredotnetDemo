name: Trigger auto deployment for dotnet-backend-api

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/dotnet-backend-api-AutoDeployTrigger-4a2679f4-5b6d-4094-ab75-b28d02df0efd.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.DOTNETBACKENDAPI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.DOTNETBACKENDAPI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.DOTNETBACKENDAPI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.DOTNETBACKENDAPI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.DOTNETBACKENDAPI_REGISTRY_PASSWORD }}
          containerAppName: dotnet-backend-api
          resourceGroup: DefaultResourceGroup-CID
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


